[package]
# The command is `gwt`; the crate needs another name because `gwt` and
# `gwt-cli` are both taken on crates.io by unrelated projects.
name = "gwt-rs"
version = "1.3.4"
edition = "2021"
rust-version = "1.85"
description = "A friendly git worktree manager with automatic paths, hooks and shell integration"
license = "MIT"
repository = "https://github.com/ktakada42/gwt"
readme = "README.md"
keywords = ["git", "worktree", "cli", "development"]
categories = ["command-line-utilities", "development-tools"]

[[bin]]
name = "gwt"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive", "unstable-ext"] }
# Dynamic completion (worktree and branch names) lives behind an unstable
# feature, so the version range is pinned to the minor it was written against.
clap_complete = { version = "~4.6", features = ["unstable-dynamic"] }
clap_mangen = "0.3"
crossterm = "0.29"
serde = { version = "1.0", features = ["derive"] }
toml = "1.1"

[dev-dependencies]
tempfile = "3"

[profile.release]
lto = true
strip = true
